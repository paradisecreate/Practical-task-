FROM nginx:alpine

COPY website/ /usr/share/nginx/html/
# Generated by GitHub Copilot
# nginx config that also proxies /api/ to the backend on the host.
COPY docker/default.conf /etc/nginx/conf.d/default.conf

EXPOSE 80